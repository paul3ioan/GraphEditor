#pragma once
#include <string>
namespace GeneralHelpers
{
	int fromStringToInt(std::string);

	char* fromStringToCharArray(std::string);

	std::string fromIntToString(int);

	char* fromIntToCharArray(int input);
};

